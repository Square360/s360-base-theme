import {{ camelCase bundle }}Twig from './{{ kebabCase bundle }}.twig';
import {{ camelCase bundle }}Data from './{{ kebabCase bundle }}.yml';
import './{{ kebabCase bundle }}.js';

import drupalAttribute from 'drupal-attribute';

/**
 * Storybook Definition.
 */
export default { title: '/{{ titleCase bundle }}' };

export const {{ camelCase bundle }} = () => {
  return {{ camelCase bundle }}Twig({
    ...{{ camelCase bundle }}Data,
    attributes: new drupalAttribute(Object.entries({{ camelCase bundle }}Data.{{ entityType }}_attributes)),
  });
}
