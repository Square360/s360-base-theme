{{ attach_library('s360_base_theme/view.display-table') }}

{% include "@ui-views/table.twig" with {
  view_caption_needed: caption_needed,
  view_caption: caption,
  view_header: header,
  view_title: title,
  view_rows: rows,
  view_responsive: responsive,
  view_sticky: sticky,
  view_summary_element: summary_element
} %}
