{% if label_hidden %}
  {% for item in items %}
    {{- item.content -}}
  {% endfor %}
{# There is a label, assume this is not for public display. #}
{% else %}
  {{ attach_library('s360_base_theme/field') }}

  {% include "@ui-field/field/field.twig" with {
    field_items: items
  } %}
{% endif %}
