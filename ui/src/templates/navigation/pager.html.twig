{{ attach_library('s360_base_theme/navigation.pager') }}

{% include "@ui-navigation/pager/pager.twig" with {
  pager_heading_id: heading_id,
  pager_first: items.first,
  pager_previous: items.previous,
  pager_ellipses_previous: ellipses.previous,
  pager_pages: items.pages,
  pager_current: items.current,
  pager_ellipses_next: ellipses.next,
  pager_next: items.next,
  pager_last: items.last
} %}
