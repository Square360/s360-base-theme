{{ attach_library('s360_base_theme/node.page.full') }}

{% include "@ui-node/page/full/component/node.page.full.twig" with {
  node_field_label: label,

  node_is_front: is_front,
  node_is_published: node.isPublished(),
} %}
