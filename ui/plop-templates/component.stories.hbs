import {{ camelCase componentName }}Twig from './{{ componentName }}.twig';
import {{ camelCase componentName }}Data from './{{ componentName }}.yml';
import './{{ componentName }}.js';

/**
 * Storybook Definition.
 */
export default { title: '{{ storybookTitle }}' };

export const {{ storybookStoryFnName }} = ({}) => {
  return {{ camelCase componentName }}Twig({
    ...{{ camelCase componentName }}Data
  });
}
