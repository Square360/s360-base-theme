import {{ componentNameToCamelCase }}Twig from './{{ componentFilenamePartial }}.twig';
import {{ componentNameToCamelCase }}Data from './{{ componentFilenamePartial }}.yml';
import './{{ componentFilenamePartial }}.js';

/**
 * Storybook Definition.
 */
export default { title: '{{ storybookTitle }}' };

export const {{ componentNameToCamelCase }} = ({}) => {
  return {{ componentNameToCamelCase }}Twig({
    ...{{ componentNameToCamelCase }}Data
  });
}
