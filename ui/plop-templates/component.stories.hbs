import {{ camelCase bundle }}Twig from './{{ kebabCase bundle }}{{#if viewMode }}.{{ viewMode }}{{/if}}.twig';
import {{ camelCase bundle }}Data from './{{ kebabCase bundle }}{{#if viewMode }}.{{ viewMode }}{{/if}}.yml';
import './{{ kebabCase bundle }}{{#if viewMode }}.{{ viewMode }}{{/if}}.js';

/**
 * Storybook Definition.
 */
export default { title: '{{ storyBookTitle }}' };

export const {{ camelCase bundle }} = () => {
  return {{ camelCase bundle }}Twig({
    ...{{ camelCase bundle }}Data
  });
}
